# Comma-separated browser origins permitted to call the API.
APP_ENV=development
CORS_ORIGINS=http://localhost:3000

# Configure locally; percent-encode special characters in credentials.
# DATABASE_URL=postgresql+psycopg://ROLE:PASSWORD@localhost:5432/traderath_dev
# TEST_DATABASE_URL=postgresql+psycopg://ROLE:PASSWORD@localhost:5432/traderath_test
